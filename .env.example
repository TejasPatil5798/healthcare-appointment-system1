# copy to .env and edit if you want email notifications or custom session secret
SESSION_SECRET=change_this_long_secret
# Optional SMTP settings:
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
# SMTP_FROM=no-reply@yourdomain.com
